/**
* @file Calculator_header.h
*
*/

#include <stdio.h>
#include <string.h>

/// Global variable defining the resultant array
int result_arr[2];

/**
* Calculates the sum of two numbers
* @param[in] Two integer numbers
* @return returns sum of two numbers
*/
int add(int, int);

/**
* Calculates the difference of two numbers
* @param[in] Two integer numbers
* @return returns difference of two numbers
*/
int add(int, int);

/**
* Calculates the sum and difference of two numbers
* @param[in] an integer array consists of two numbers
* @return array containing the sum and difference of two numbers
*/

int *calculate(int arr[]);

